import { ApilocaleMiddleware } from './apilocale.middleware';

describe('ApilocaleMiddleware', () => {
  it('should be defined', () => {
    expect(new ApilocaleMiddleware()).toBeDefined();
  });
});
